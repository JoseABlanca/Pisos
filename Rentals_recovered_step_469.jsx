"import { useState, useEffect } from 'react';\nimport { db } from '../firebase/config';\nimport { collection, query, where, onSnapshot, doc, setDoc, deleteDoc } from 'firebase/firestore';\nimport { useAuth } from '../context/AuthContext';\nimport Window from '../components/Window';\nimport { \n  Check, X, Search, Plus, Trash2, Edit, Save, \n  FileText, Building2, User, Key, Users\n} from 'lucide-react';\nimport AccountSelector from '../components/AccountingEntryModal'; // Assuming this exists or similar\n\nexport default function Rentals() {\n  const { user, queryUserIds } = useAuth();\n  const [rentals, setRentals] = useState([]);\n  const [properties, setProperties] = useState([]);\n  const [customers, setCustomers] = useState([]);\n  \n  const [showForm, setShowForm] = useState(false);\n  const [isEditing, setIsEditing] = useState(false);\n  const [isMobile, setIsMobile] = useState(window.innerWidth < 768);\n  const [searchQuery, setSearchQuery] = useState('');\n  \n  const [showModalSidebar, setShowModalSidebar] = useState(true);\n  const [activeFormTab, setActiveFormTab] = useState('general');\n\n  const initialFormState = {\n    reference: '',\n    propertyId: '',\n    tenantId: '',\n    status: 'activo',\n    rentalType: 'vivienda habitual',\n    duration: 'fijo',\n    rentAmount: 0,\n    depositAmount: 0,\n    paymentPeriod: 'mensual',\n    incomeAccountId: '',\n    expenseAccountId: '',\n    notes: ''\n  };\n\n  const [formData, setFormData] = useState(initialFormState);\n\n  useEffect(() => {\n    const handleResize = () => setIsMobile(window.innerWidth < 768);\n    window.addEventListener('resize', handleResize);\n    return () => window.removeEventListener('resize', handleResize);\n  }, []);\n\n  useEffect(() => {\n    if (!user) return;\n    \n    const userIds = queryUserIds?.length > 0 ? queryUserIds : [user.uid];\n    \n    const unsubRentals = onSnapshot(\n      query(collection(db, 'rentals'), where('userId', 'in', userIds)),\n      (snap) => {\n        setRentals(snap.docs.map(d => ({ ...d.data
<truncated 14914 bytes>